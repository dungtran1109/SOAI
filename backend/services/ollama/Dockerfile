# Use the official Ollama image as the base image
FROM ollama/ollama

# Set working directory (optional, if additional configuration is needed)
WORKDIR /root/.ollama

# Expose the default port for Ollama
EXPOSE 11434

# Set the default command (already part of the official image, included here for clarity)
# CMD ["ollama","start"]
